{% extends 'base.html.twig' %}

{% block title %}  Edit Password {% endblock %}

{% block body %}
   <div class="container clearfix">
      <h1 class="my-5">Edit Password</h1>

      {{ form_start(form) }}

      {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary float-right">
         <i class="fas fa-check"></i>
           Change Password
        </button>
      {{ form_end(form) }}
   </div>
{% endblock %}
